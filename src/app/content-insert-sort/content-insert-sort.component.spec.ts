/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ContentInsertSortComponent } from './content-insert-sort.component';

describe('Component: ContentInsertSort', () => {
  it('should create an instance', () => {
    let component = new ContentInsertSortComponent();
    expect(component).toBeTruthy();
  });
});
